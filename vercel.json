{
  "version": 2,
  "functions": {
    "api/webhook.js":   { "maxDuration": 10 },
    "api/keepalive.js": { "maxDuration": 10 }
  },
  "crons": [
    {
      "path": "/api/keepalive",
      "schedule": "0 10 */4 * *"
    }
  ],
  "routes": [
    { "src": "/webhook",      "dest": "/api/webhook.js"   },
    { "src": "/api/keepalive","dest": "/api/keepalive.js" }
  ]
}